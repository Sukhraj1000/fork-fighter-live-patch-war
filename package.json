{
  "name": "daytona-hackathon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "pnpm --filter @fork-fighter/server dev",
    "play": "pnpm run build:e2e && pnpm --filter @fork-fighter/server dev",
    "demo:seeded": "pnpm run build:e2e && GAME_MASTER_PROVIDER=mock MATCH_PATCH_CADENCE_MS=5000 MATCH_PROPOSAL_DEADLINE_MS=2000 MOCK_AGENT_DELAY_MS=500 MOCK_MUTATION_DURATION_MS=8000 pnpm --filter @fork-fighter/server dev",
    "demo:live": "pnpm run build:e2e && GAME_MASTER_PROVIDER=daytona MATCH_PATCH_CADENCE_MS=21000 MATCH_PROPOSAL_DEADLINE_MS=20000 pnpm --filter @fork-fighter/server dev",
    "daytona:smoke": "node --import tsx src/index.ts",
    "build": "pnpm run build:root && pnpm --recursive --if-present run build",
    "build:e2e": "pnpm --filter @fork-fighter/web build",
    "build:root": "tsc -p tsconfig.json",
    "typecheck": "pnpm run typecheck:root && pnpm --recursive --if-present run typecheck",
    "typecheck:root": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --recursive --if-present run test",
    "test:e2e": "pnpm run build:e2e && playwright test",
    "start:e2e": "node --import tsx tests/e2e/server.ts",
    "check": "pnpm run typecheck && pnpm run test && pnpm run build",
    "verify": "pnpm run check && pnpm run test:e2e"
  },
  "dependencies": {
    "@fork-fighter/gm-orchestrator": "workspace:*",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.4.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2"
  }
}
