{
  "name": "@fork-fighter/gm-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare:snapshot": "tsx src/daytona/prepare-snapshot-cli.ts",
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.test.json --noEmit"
  },
  "dependencies": {
    "@daytona/sdk": "^0.207.0",
    "@fork-fighter/contracts": "workspace:*",
    "dotenv": "^17.4.2",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^26.4.0"
  }
}
